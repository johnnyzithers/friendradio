const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const MediaLogSchema = new Schema({
    // TODO: create mediaObject
    // id
    // name
    // uploadedBy
    // metaData ( mp3 meta data )
});